export const INITIAL_STATE = {
    projects: {
        Globo: ['Play', 'Combate']
    }
}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state
    }
}